import React from 'react';
import LocationSection from './Cities/LocationSection';

var Page = React.createClass({
  render() {
    return (
      <LocationSection />
    );
  }
});

export default Page;
